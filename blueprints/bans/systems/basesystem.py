
def getbans(user):
    """ 
    Should return a dict containing info about the user.
    It should contain the following info:
      -   int : bancount
      - O dict : bans
          -   string : banreason
          -   ...
    """
    return {"bancount" : 0}


def fulllookup(user):
    """
    Should return a dict containing info about the user.
    It should be containing the following info:
      -   int : bancount
      - O dict : bans
          -   string : banreason
          -   ...  
      - O int : playerrep - 0-10, the higher the better
      - O dict : altlist
          -   string : info - format is "username numbans"
          -   ...
    """
    return {"bancount" : 0}


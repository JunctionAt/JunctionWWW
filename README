The site code.

Launch your own with uwsgi_python:

uwsgi_python --http :900x -w app:application


You need to create a local_config.py file that includes ENGINE for a mysql connection string and any other configs you want for your test environment:

cat <<EOF> local_config.py
from sqlalchemy import create_engine
DEBUG = True
ENGINE = create_engine('mysql://wiggitywhack:wiggitywhack@localhost/wiggitywhack', echo=True)
EOF